# n8n 环境变量配置模板
# 复制此文件为 .env 并填入实际值

# ============================================
# 云函数配置
# ============================================

# 云函数URL（微信云开发）
# 格式：https://你的环境ID.service.tcloudbase.com
CLOUD_FUNCTION_URL=https://cloudbase-0gu1afji26f514d2.service.tcloudbase.com

# NAS密钥（用于认证，自定义32位随机字符串）
# 生成方式：openssl rand -hex 16
NAS_SECRET_KEY=请填入32位随机密钥

# ============================================
# AI服务配置
# ============================================

# Gemini API密钥
# 获取地址：https://makersuite.google.com/app/apikey
GEMINI_API_KEY=请填入你的Gemini_API密钥

# Gemini API URL（可选，默认使用官方API）
GEMINI_API_URL=https://generativelanguage.googleapis.com/v1beta

# ============================================
# 背景移除配置
# ============================================

# 是否启用背景移除（true/false）
ENABLE_BG_REMOVAL=true

# Rembg服务地址（如果使用Docker部署在本机）
REMBG_HOST=localhost

# Rembg服务端口
REMBG_PORT=7000

# 或者使用在线服务（二选一）
# REMBG_API_URL=https://www.rembg.com/api/remove

# ============================================
# n8n配置
# ============================================

# n8n执行超时（-1表示无限制）
EXECUTIONS_TIMEOUT=-1

# n8n最大并发执行数
N8N_CONCURRENCY_PRODUCTION=10

# n8n时区
GENERIC_TIMEZONE=Asia/Shanghai

# ============================================
# 可选：Remove.bg API（商业服务）
# ============================================

# Remove.bg API Key（每月50张免费）
# REMOVE_BG_API_KEY=你的API_Key
